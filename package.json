{
  "name": "freesurf-to-post",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-posting API — compose once, publish everywhere",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260718.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.112.0"
  }
}
