# Archive

Superseded material kept for reference. The **in-app API tab** (dashboard → API)
and `dashboard/llms.txt` are the source of truth for the API.

- `API_REFERENCE.md` — older API doc (base `post.cnxt.to`, JWT-first framing).
  Replaced by the in-app API tab and `dashboard/llms.txt`.
- `legacy/` — dead / unused code moved out of `src/` (see `legacy/README.md`).
