name = "freesurf-to-post"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# Routes: Worker only handles API paths so Pages can serve the dashboard at root
routes = [
  { pattern = "post.freesurf.tools/api/*", zone_name = "freesurf.tools" }
]

# KV for rate limiting counters
[[kv_namespaces]]
binding = "RATE_LIMITS"
id = "0a83c67d64d5484e96cb732f64e19c89"

[vars]
ENVIRONMENT = "production"
