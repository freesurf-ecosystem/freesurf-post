# Dependencies
node_modules/
mobile/node_modules/
mobile/.expo/
mobile/android/

# Local test media (not part of the product)
/test/

# EAS / store signing credentials (secret - never commit)
credentials.json
credentials/
*.jks
*.p12
*.keystore
*.mobileprovision

# Local env / secrets — never commit
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
*.local
